import { Background } from '../background';
import { Form } from '../form';

export const HomePage: React.FC = () => {
    return (
        <>
            <Background />
            <Form />
        </>
    );
};
